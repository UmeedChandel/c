/*
*Input:
*The first line of the input contains T denoting the number of testcases.
*First line of eacg test case contains two space separated elements, N 
*denoting the size of the array and an integer D denoting the number size
*of the rotation. Subsequent line will be the N space separated array elements.
*
*Output:
*For each testcase, in a new line, output the rotated array.
*/


#include <stdio.h>
#include <stdlib.h>

int main()
{

    int n;
    scanf("%d",&n);
    printf(" \n");
    int l,s;

    int t=l-s;
    for(int x=0;x<n;x++)
    {
    scanf("%d %d",&l,&s);
    int a[l],b[s];

    for(int i=0;i<l;i++)
    {
        scanf("%d",&a[i]);
    }

    for(int j=0;j<s;j++)
    {
        b[j]=a[j];
    }


    for(int i=0;i<(l-s);i++)
    {
        a[i]=a[i+s];
    }


    int o=0;
    for(int j=(l-s);j<l;j++)
    {
       a[j]=b[o];
       o++;
    }

    printf("\n OUTPUT \n");


    for(int i=0;i<l;i++)
    {
        printf("%d ",a[i]);
    }
     printf(" \n");


}
}








